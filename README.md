# Назаров Кирилл Алексеевич ИТС-2 Лабораторная №8

# Задание 1-3

### Текст задачи

Разработать консольное приложение с дружественным интерфейсом с возможностью выбора

заданий для работы с «базой данных (БД)», хранящейся в бинарном файле. Перечень полей

(минимум 5), достаточно полно характеризующих заданную в варианте предметную область,

предложить самостоятельно (постараться отразить в перечне полей такие, которые требуют разных

типов данных). Приложение должно выполнять следующие функции:

1\. Чтение базы данных из бинарного файла.

2\. Просмотр базы данных.

3\. Удаление элементов (по ключу).

4\. -

5\. -

В классе должны присутствовать свойства, конструкторы, перегруженный метод ToString(). Весь

функционал приложения реализовать в виде методов вспомогательного класса с помощью LINQзапросов.

Предусмотреть обработку возможных ошибок при работе программы.

Пункт 5 оценивается в 2 балла, остальные пункты по 1 баллу. Максимально за лабораторную работу

можно получить 6 баллов.

8 Вариант Каталог продукции



Алгоритм решения

1. Объявляем поле класса Бд предметов
2. попадаем в мейн в нем выводим дружелюбный интерфейс 
3. Создаем обьект класса Бд предметов и сразу через метод подрузки подружаем содержимое файла 
4. т.к файл бинарный читаем его через встроенный метод и переводим (перекодируем) в json
5. пытаемся читать json файл также через встроенный класс json
6. если он содержит предметы подгружаем их если нет создаем новый список 
7. также выводим в консоль количество предметов в бд
8. также обрабатываем ошибки 
9. &#x20;после отработки вспомогательного класса возвращаемся в main 
10. и даем пользователю на выбор дружелюбное меню с меню выбора работы Бд
11. при воборе 1 просмотр обьектов попадаем в вспомогательный класс проверяем есть ли предметы если да сортируем через лик запросы и выыводим количество предметов 
12. при добавлении предметов присваиваем ему уникальный id, ручками пишем название, категорию, цену и количество также автоматом присваивается дата добавления 
13. &#x20;id получает предмет посредствам встроенной сортировки ти есть макс +1 
14. теперь создаем обьект класса через конструктор передавая полученные переменные и сохраняем результат 
15. удаление обьетка из пула по его id
16. работает аналогично добавлению показываем все какие предметы есть потом просим id у пользователя какой необходимо удалить, начинаем сравнивать через линк запрос все id обьектов и через метод where создаем новый список без удалённого  







Тестирование

